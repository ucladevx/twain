import styled from "styled-components";

const Wrapper = styled.div`
  display: grid;
  font-family: Cabin;
  font-style: normal;
  line-height: normal;
  overflow: hidden;
`;

export default Wrapper;
